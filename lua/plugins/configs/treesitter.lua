local options = {
  ensure_installed = { "lua", "javascript", "tsx", "typescript", "rust", "css" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
